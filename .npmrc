# pnpm 11+ only reads auth/registry settings from .npmrc — engine-strict,
# save-exact, and allowBuilds live in pnpm-workspace.yaml instead.

# @nvcct packages (e.g. db-entities) are published to GitHub Packages, not the
# public npm registry. Auth is per-developer: add a PAT with `read:packages`
# scope to your *global* ~/.npmrc (never commit a token here):
#   //npm.pkg.github.com/:_authToken=<token>
@nvcct:registry=https://npm.pkg.github.com
